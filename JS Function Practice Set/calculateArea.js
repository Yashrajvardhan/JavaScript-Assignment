/* 8. Create a function calculateArea that takes length and width as
parameters and returns the area of a rectangle. */
const rectangleArea=(a ,b)=> a*b

const calculateArea=(length,width)=> rectangleArea(length ,width)

console.log(calculateArea(10,20))