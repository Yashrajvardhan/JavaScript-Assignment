/* 7. Merge the arrays ['Raj', 'Amit'] and ['Sita', 'Geeta'] into one. */

const arr1 = ['Raj', 'Amit'];
const arr2 = ['Sita', 'Geeta'];

const merged = arr1.concat(arr2);
console.log(merged);  // Output: ['Raj', 'Amit', 'Sita', 'Geeta']
